[package]
name = "worker-candle"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
candle-core = { version = "0.4", features = ["cuda"] }
candle-nn = "0.4"
shared-ipc = { path = "../shared-ipc" }
